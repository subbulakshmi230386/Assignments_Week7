package testcases;
import org.testng.annotations.Test;

import base.SeviceCommonMethods;

import page.ServiceLogin;

public class SeviceOutput extends  SeviceCommonMethods
{
@Test
public void serviceresult() throws InterruptedException
{
	new ServiceLogin(driver).enterUserName()
	.enterPassWord().clickLogin().Incident().MandatoryFileds().Verify();
}
}
